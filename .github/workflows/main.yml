name: Unity CI/CD - Main push (Format check, Test Runner)

on:
  push:
    branches: [ "main" ]
    
jobs:
  Unity_CICD_MAIN:
    runs-on: ubuntu-latest
    steps:
      - name: "Calling format check..."
        uses: ./.GIT-TEST/.github/workflows/basic/format.yml
      - name: "Calling test runner..."
        uses: ./.GIT-TEST/.github/workflows/basic/test.yml
